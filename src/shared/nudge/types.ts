/** Backward-compat barrel — re-exports from domain locations. */
export type { ContextInput, NudgeContext } from '../../domain/nudgeContext';
export type { NudgeRule, SafetyRule, NudgeEvaluation } from '../../domain/nudgeTypes';
